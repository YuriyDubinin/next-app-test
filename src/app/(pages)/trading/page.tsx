'use client';

import { useRouter } from 'next/navigation';

export default function Trading() {
    const router = useRouter();
    return (
        <div>
            <h1>Trading</h1>
        </div>
    );
}
